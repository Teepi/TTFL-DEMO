<select class="form-select" aria-label="Select a date">
    @{
        foreach (var item in @Model)
        {
            <option value="@item.Key">@item.Key - @item.Value</option>
        }
    }
</select>